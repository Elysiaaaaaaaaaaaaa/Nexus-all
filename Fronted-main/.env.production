# 普通 vite build（网站 + Android Capacitor）；与 .env.tauri、代码内默认一致
# API：/api/... ；静态视频：同主机 /videos/...（FastAPI 挂载 user_files）
VITE_API_BASE_URL=http://101.200.1.56
# 显式指定视频源站（与 API 同机时与上一行一致；若视频单独域名可只改此项）
VITE_VIDEO_ORIGIN=http://101.200.1.56
